# final-project-TutorConnect
1. to run the project navigate through CD to the backend directory type "npm install" and then "npm run dev" in terminal
2. navigate through CD to the frontend directory and type "npm install" then "npm run start" in terminal

note: for developers make sure you have right env file
